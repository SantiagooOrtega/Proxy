services:
  - type: web
    name: ai-proxy-platform
    runtime: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/ai-proxy-platform-0.0.1-SNAPSHOT.jar
    envVars:
      - key: PORT
        value: 8080
